# 🗓 FRI 5 JUN — A2.1 NPC Character + Spawner (Day 1)

## 1. Create ANPCCharacter (Blueprintable)
- [ ] `Source/Onset/AI/ANPCCharacter.h` — `UCLASS(Blueprintable)`, inherit `ACharacter` (already Blueprintable), `GENERATED_BODY()`
- [ ] `Source/Onset/AI/ANPCCharacter.cpp` — default constructor, attach tags (e.g. `"Enemy"`)
- [ ] Add basic mesh component + simple collision setup

## 2. Create ANPCAIController (stub, Blueprintable)
- [ ] `Source/Onset/AI/ANPCAIController.h` — `UCLASS(Blueprintable)`, inherit `AAIController` (already Blueprintable), `GENERATED_BODY()`
- [ ] `Source/Onset/AI/ANPCAIController.cpp` — empty stub (will drive StateTree later)

## 3. Create FSpawnConfig Struct (BlueprintType)
- [ ] `Source/Onset/Spawning/SpawnConfig.h`
- [ ] `USTRUCT(BlueprintType)` Fields: `TSubclassOf<ANPCCharacter> EnemyClass`, `int32 GroupSize`, `float SpawnRadius`, `float RespawnDelay`

## 4. Create AEnemySpawner Base (Blueprintable)
- [ ] `Source/Onset/Spawning/AEnemySpawner.h` — `UCLASS(Blueprintable)`, inherit `AActor` (already Blueprintable)
- [ ] `UPROPERTY(EditAnywhere)` `FSpawnConfig SpawnConfig`
- [ ] `UPROPERTY(EditAnywhere)` `TArray<AActor*> SpawnPoints`
- [ ] Declare `SpawnGroup()`, `DestroyGroup()`, `GetSpawnLocation(int32 Index)`
- [ ] `.cpp` — implement skeleton definitions

## 5. Test
- [ ] Compile — verify no errors
- [ ] Place spawner in test level
- [ ] Press Play — stub spawner exists but doesn't spawn yet (completed tomorrow)

## ✅ Done
- [ ] Track progress toward **A2.1** in `TODO/Private_Demo_Checklist.md`
