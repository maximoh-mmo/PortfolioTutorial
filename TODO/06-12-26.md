# 🗓 FRI 12 JUN — A3 Buffer / Overflow

## ✅ Done

### A3.2 AI Perception
- [x] Verify perception triggers on player proximity — **confirmed working** (all NPCs acquire and track player target via `OnPerceptionUpdated`)
- [x] Hearing/assist deferred to A4 (requires GAS noise events)

### A3.3 Behaviour States
- [x] **Idle** — timer-based, `FOnsetStateTreeIdleTask`
- [x] **Roam** — nav-reachable territory patrol, `FOnsetStateTreeRoamTask`
- [x] **Agro** — face target via `SetFocus`, facing-angle check, `FOnsetStateTreeAgroTask`
- [x] **Lost** — clear focus, random pause, `FOnsetStateTreeLostTargetTask`
- [x] **Chase** — `MoveToActor`, auto-follows target, returns `Succeeded` on arrival → transition routing, `FOnsetStateTreeChaseTask`
- [x] **Marooned** — leash-broken chase (same `ChaseTask` in asset, no leash-gated transition)

### Task Base Struct (migration)
- [x] **`FOnsetStateTreeTaskBase`** — `: FStateTreeTaskCommonBase` with static helpers:
  - `GetController(Context)` — logged Cast to `AOnsetAIController`
  - `GetTarget(Context)` — logged, guarded null
  - `GetSelfBaseCharacter(Context)` — logged Cast to `AOnsetBaseCharacter`
  - `GetPathFollowingComponent(Context)` — logged, guarded null
  - `HasMoveCompleted(Context)` — guards + `DidMoveReachGoal()`
- [x] All 5 task structs migrated: Agro, Chase, Idle, LostTarget, Roam
- [x] Dead includes cleaned per file (duplicates from base header)

### Data Model
- [x] `HomeLocation` moved from `AOnsetEnemy` → `AOnsetBaseCharacter` (player can reuse for respawn)
- [x] `OnsetEnemy.h` cleaned — no more `HomeLocation` property

### Transition Condition
- [x] **`FOnsetStateTreeDistanceCondition`** — reusable condition with:
  - `EOnsetStateTreeDistanceSource` (CurrentTarget, HomeLocation)
  - `UE::StateTree::EComparisonOperator`
  - `DistSquared` + squared threshold (avoids sqrt)
  - `bAllowNoTarget` flag for Lost-range fallback
- [x] Compiles and wires in asset

### Documentation
- [x] `Private_Demo_Checklist.md` updated — Chase, Base, Condition, Marooned checked; counter 27
- [x] `NPC_AI_System.md` updated — base struct, ChaseTask, DistanceCondition, new diagram
- [x] `06-12-26.md` — this file

## Remaining A3.3
- [ ] **Attack** — stub GA call (real GAS in A4)
- [ ] **Flee** — low health retreat
- [ ] **RoamWander** — no-home stray patrol with timed despawn
- [ ] A3.4 Group Assist (deferred until hearing integration)
- [ ] A3.5 Player AI Autoplay
