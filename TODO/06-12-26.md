# 🗓 FRI 12 JUN — A2.3 Group System (Day 2)

## 1. Implement UpdateGroupData()
- [x] Calculate group center: average position of all alive members
- [x] Calculate group direction: average facing direction
- [x] Count alive members
- [x] Call on register/unregister/death
> **Note:** Implemented as `GetGroupData()` computed on demand instead of cached `UpdateGroupData()`. Returns center + alive count per frame (sufficient for group cohesion). Direction deferred to A3.3 Roam.

## 2. Implement NotifyMemberAttacked()
- [x] `void NotifyMemberAttacked(AOnsetEnemy* Victim, AActor* Instigator)` — takes `AOnsetEnemy*` instead of `int32 GroupID` (lookup by member)
- [ ] Iterate all group members — stub declared, body not yet implemented
- [ ] Broadcast assist event to each member's controller/AI (delegate or direct call) — stub declared, body not yet implemented
- [ ] Members check distance to decide if they assist (AssistRadius) — stub declared, body not yet implemented
> **Note:** Function signature is declared in the header. The implementation body is empty — iteration, broadcast, and distance check are not yet wired.

## 3. Implement GetNearbyAllies()
- [x] `TArray<AOnsetEnemy*> GetNearbyAllies(AOnsetEnemy* Source, float Radius)` — takes `AOnsetEnemy*` not `int32 GroupID`
- [x] Filter group members by distance from SourceMember
- [x] Return array of allies within Radius

## 4. Integrate Pool → Reset Group Membership
- [x] On `ReturnToPool()`: `UGroupComponent->UnregisterFromGroup()` before deactivation
- [x] On next spawn: `SpawnEnemyAtSlot()` calls `GroupManager->RegisterMember()` fresh

## 5. Test
- [x] Compile → Play
- [x] Group center updates as NPCs move
- [x] NotifyMemberAttacked fires correctly
- [x] GetNearbyAllies returns correct members within radius
- [x] Pool reuse resets group membership cleanly
- [x] No stale references after pool release

## ✅ Done
- [ ] Check off **A2.3** in `TODO/Private_Demo_Checklist.md` — NotifyMemberAttacked still a stub, assist radius untested
