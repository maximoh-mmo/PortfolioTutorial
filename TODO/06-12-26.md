# 🗓 FRI 12 JUN — A2.3 Group System (Day 2)

## 1. Implement UpdateGroupData()
- [ ] Calculate group center: average position of all alive members
- [ ] Calculate group direction: average facing direction
- [ ] Count alive members
- [ ] Call on register/unregister/death

## 2. Implement NotifyMemberAttacked()
- [ ] `void NotifyMemberAttacked(int32 GroupID, AActor* Attacker, AActor* AttackedMember)`
- [ ] Iterate all group members
- [ ] Broadcast assist event to each member's controller/AI (delegate or direct call)
- [ ] Members check distance to decide if they assist (AssistRadius)

## 3. Implement GetNearbyAllies()
- [ ] `TArray<AActor*> GetNearbyAllies(int32 GroupID, AActor* SourceMember, float Radius)`
- [ ] Filter group members by distance from SourceMember
- [ ] Return array of allies within Radius

## 4. Integrate Pool → Reset Group Membership
- [ ] On `ResetNPC()`: if NPC has UGroupComponent, call `UnregisterMember` before returning to pool
- [ ] On next `GetNPC()`: spawner assigns new GroupID, registers fresh

## 5. Test
- [ ] Compile → Play
- [ ] Group center updates as NPCs move
- [ ] NotifyMemberAttacked fires correctly
- [ ] GetNearbyAllies returns correct members within radius
- [ ] Pool reuse resets group membership cleanly
- [ ] No stale references after pool release

## ✅ Done
- [ ] Check off **A2.3** in `TODO/Private_Demo_Checklist.md`
