# 🗓 WED 1 JUL — A5.2 Replication: NPC Movement + Health

## Day 3 — Movement + Attribute Replication
- [x] Add GetLifetimeReplicatedProps to AOnsetBaseCharacter
  - Replicates bIsAlive with OnRep_bIsAlive (sets hidden/collision on client)
- [x] Set SetReplicateMovement(true) in AOnsetEnemy constructor
- [x] Build — compiles clean

## GAS Attribute Verification
- [x] Health/MaxHealth already replicate — verified in PIE log (no errors)
- [x] MovementSpeed already replicates — verified in PIE log (no errors)
- [ ] Confirm client sees health changes when NPC takes damage

## Fixes Applied
- [x] `AutoCombatController->Rename` crash — removed the unnecessary Rename call
- [x] DynamicSpeedModifier GAS replication error — replaced NewObject GE with
      proper UOnsetMovementSpeedModifierEffect class (SetByCaller magnitude)

## Session 06-28 Fixes: Visual Replication + Respawn + Validation

### NPC Visuals Now Replicate
- [x] `VisualProfile` on `AOnsetEnemy` → `ReplicatedUsing=OnRep_VisualProfile`
- [x] `OnRep_VisualProfile()` calls `ApplyProfile(VisualProfile)` on client
- [x] `GetLifetimeReplicatedProps` — `DOREPLIFETIME(AOnsetEnemy, VisualProfile)`
- [x] Removed `HasAuthority()` guard from `ApplyProfile` — client needs to run it
- [x] Clients now see the correct skeletal mesh, anim BP, and material on NPCs

### Player Respawn Fix
- [x] `AOnsetPlayerCharacter::RespawnPlayer` — added `OnRespawn()` call
- [x] `bIsAlive=true` now replicates to client → pawn becomes visible again
- [x] Camera no longer falls through world after death

### Cube Fallback Removed
- [x] `CubeVisual` component removed from `AOnsetEnemy` — no more runtime `NewObject<UStaticMeshComponent>`
- [x] `ApplyProfile` simplified to skeletal-mesh-only path
- [x] `AOnsetCorpse` — removed default cube mesh from constructor (no `/Engine/BasicShapes/Cube.Cube`)
- [x] Corpses only spawn if `VisualProfile->CorpseMesh` is assigned (optional `TSoftObjectPtr<UStaticMesh>`)

### VisualProfile Validation
- [x] Added `IsDataValid()` override — editor blocks save/cook if `SkeletalMesh` is null
- [x] Added `checkf` guard in `ApplyProfile` — crashes in Dev on incomplete profile
- [x] `checkf` allows null profile (pool recycle path), rejects non-null + missing mesh

## Verify
- [x] Project compiles (build succeeded)
- [ ] NPC movement visible on client (no jitter) — needs PIE test
- [ ] Client sees NPC health bar updates — needs PIE test
- [ ] Client sees NPC visuals — needs PIE test
