# 🗓 FRI 19 JUN — Sprint Day 7: Player AI Autoplay (Part 2)

## 1. Player AI Tasks
- [ ] Create `Public/AI/Tasks/PlayerAITasks.h/.cpp` (all player AI tasks in one file — minimal)
- [ ] **FindTargetTask** — `EnterState` runs C++ navpath-cost loop: iterate `AOnsetEnemy` actors, coarse distance filter (3000), `UNavigationSystemV1::GetPathCost`, pick lowest cost → set `TargetActor` in context
- [ ] **MoveToTask** — `EnterState` calls `MoveToActor(TargetActor)` with attack-range acceptance radius; `Tick` checks arrival
- [ ] **AttackTask** — `EnterState` calls `TryActivateAbilityByClass(GA_BasicAttack)`; `Tick` checks cooldown tag → exit when clear

## 2. Player AI StateTree Asset
- [ ] Create `Content/Game/AI/PlayerAI_StateTree` BP asset
- [ ] Schema: `UPlayerAIStateTreeSchema`
- [ ] States: Idle → FindTarget → MoveToTarget ↔ Attack
- [ ] Conditions: HasTarget, InAttackRange
- [ ] Exits: target dead → Idle, out of range → MoveToTarget, lost target → Idle

## 3. Verify
- [ ] Compile → Play
- [ ] Toggle autoplay on → AI takes over, seeks enemies, moves, attacks
- [ ] Toggle autoplay off → player regains control
- [ ] AI picks nearest enemy by navpath
- [ ] AI handles target death → reacquires next enemy
- [ ] PvP auto-disabled → AI never targets other players
- [ ] Abilities fire correctly under AI control
- [ ] No crash on toggle during any StateTree state

## ✅ Done
- [ ] (filled in after work)

## What's Next
- Verification Pass (all remaining unchecked A3.1, A3.2, A3.4, A4.5b items)
- Sprint retrospective
