# 🗓 TUE 9 JUN — A2.2 Object Pooling (Day 1)

## 1. Create ANPCPoolManager Class (Blueprintable)
- [ ] `Source/Onset/Spawning/ANPCPoolManager.h` — `UCLASS(Blueprintable)`, inherit `AActor` (already Blueprintable), singleton pattern
- [ ] Private pool: `TArray<ANPCCharacter*> ObjectPool`
- [ ] Config: `int32 PoolSize`, `TSubclassOf<ANPCCharacter> PoolClass`

## 2. Implement Pre-Allocation
- [ ] In `BeginPlay()`: spawn `PoolSize` NPCs, add to pool, hide/deactivate each
- [ ] `SpawnActor` with `ESpawnActorCollisionHandlingMethod::AlwaysSpawn`
- [ ] Store all in `ObjectPool`

## 3. Implement GetNPC()
- [ ] Find first valid, inactive NPC in pool
- [ ] Activate it (set actor hidden = false, enable tick, etc.)
- [ ] Return the NPC reference
- [ ] If pool exhausted: `SpawnActor` as fallback + log warning

## 4. Implement ReleaseNPC()
- [ ] Deactivate NPC (set hidden, disable tick, clear AI state)
- [ ] Return to pool array
- [ ] Do NOT destroy the actor

## 5. Test
- [ ] Compile → Play
- [ ] Pool pre-allocates NPCs at game start
- [ ] GetNPC() returns a pooled NPC
- [ ] ReleaseNPC() deactivates and returns it
- [ ] Pool exhaustion fallback creates new NPC

## ✅ Done
- [ ] Track progress toward **A2.2** in `TODO/Private_Demo_Checklist.md`
