# 🗓 TUE 9 JUN — A3.4 Group Assist Integration

## 1. Add Assist Event Input to StateTree Context
- [ ] Add `bAssistTriggered` bool to StateTree context data
- [ ] Add `AssistTarget` (AActor*) to context (the instigator who attacked the ally)
- [ ] Wire `OnPerceptionUpdated` hearing path → sets `bAssistTriggered` + `AssistTarget`

## 2. Implement Assist State Transition
- [ ] Create StateTree transition: Agro → Assist (`bAssistTriggered` == true)
- [ ] Assist state: face + move toward `AssistTarget` (reuse Chase logic with new target)
- [ ] On arrival at assist range → transition to Attack
- [ ] Clear `bAssistTriggered` on state exit

## 3. Verify
- [ ] Compile → Play
- [ ] Place two NPCs in same group, player attacks one
- [ ] Second NPC hears noise → `OnPerceptionUpdated` fires → assist flag set → transitions to Assist
- [ ] Second NPC moves toward player and attacks
- [ ] Verify no assist when attacker is out of `HearingRange` (per profile)
- [ ] Verify no assist when attacker is another NPC (ignored)

## ✅ Done
- [ ] Check off **A3.4** in `TODO/Private_Demo_Checklist.md`
