# 🗓 TUE 9 JUN — A2.2 Object Pooling (Day 1)

## 1. Create AOnsetPoolManager Class
- [x] `Source/Onset/Public/Spawning/OnsetPoolManager.h` — `UCLASS()`, inherit `AActor`
- [x] Private pool: `TArray<AOnsetEnemy*> ObjectPool`
- [x] Config: `int32 PoolSize` (hardcoded `AOnsetEnemy::StaticClass()` — no `PoolClass` property)
> **Design change:** Not a singleton (spawner holds reference). No Blueprintable. Hardcoded enemy class since all NPCs share the same base.

## 2. Implement Pre-Allocation
- [x] `InitializePool()` called from spawner, not `BeginPlay()` (lazy init via `bPoolInitialized` flag)
- [x] `SpawnActor` with `ESpawnActorCollisionHandlingMethod::AlwaysSpawn`
- [x] Store all in `ObjectPool`, deactivated via `ReturnToPool()`

## 3. Implement GetPooledEnemy()
- [x] Find first valid, deactivated NPC (`IsHidden()`) in pool
- [x] `ActivateEnemy()` — unhide, enable tick, enable collision
- [x] Return the NPC reference
- [x] If pool exhausted: `SpawnActor` as fallback + add to pool

## 4. Implement ReleasePooledEnemy() / ReturnToPool()
- [x] `ReturnToPool()`: reset location, profile, collision, tick, input; call `ApplyProfile(nullptr)`
- [x] Add to pool array if not already there
- [x] Do NOT destroy the actor

## 5. Test
- [x] Compile → Play
- [x] Pool pre-allocates NPCs at game start
- [x] `GetPooledEnemy()` returns a pooled NPC
- [x] `ReleasePooledEnemy()` deactivates and returns it
- [x] Pool exhaustion fallback creates new NPC

## ✅ Done
- [x] Track progress toward **A2.2** in `TODO/Private_Demo_Checklist.md`
