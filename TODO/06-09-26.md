# 🗓 TUE 09 JUN — SearchTask + A4.6 Scope Revision

## ✅ Done

### A3.4 — SearchTask
- [x] `FOnsetStateTreeSearchTask` — full C++ implementation
- [x] Yaw sweep (oscillating focal point), cone-restricted point picking, MinCycles + MaxSearchDuration exit
- [x] StateTree wired: Investigate → Search → Idle + interrupt to Agro on target sighted
- [x] Fixed `USTRUCT(BlueprintType)` compile error (removed `BlueprintReadOnly`, matching existing pattern)

### A4.6 — Scope Discussion & Revision
- [x] **No dash ability** → replaced by **Shadowstep** passive (on-kill blink behind nearest enemy, cooldown-gated)
- [x] **No projectile** → ranged attacks are LOS+Distance single target with particle effects (not a separate ability)
- [x] **Activated abilities (2 slots):** AoE (target-centered) + Cone (directional)
- [x] **Shadowstep** is passive — not an ability slot, triggered by death event
- [x] Shadowstep target resolution: behind nearest visible enemy within distance gate
- [x] Class-specific? Yes (deferred — class system not roughed out yet)
- [x] NPC side TBD — whether NPCs also use AoE/Cone abilities or only basic attack

### Pool Cleanup Discussion
- [x] `OnUnPossess` doesn't reset pawn `MaxWalkSpeed` — speed leaks across pool cycles
- [x] No UE "re-run constructor" for existing actors
- [x] Fix deferred to Episode 22 (GAS Movement Speed Attribute — GE-based cleanup)
- [x] Alternative: remove all active GEs on pool return

## A4.6 — Implementation Plan (rough)

### New C++ files
- `GA_OnsetAoE.h/.cpp` — target-centered AoE GA
- `GA_OnsetCone.h/.cpp` — directional frontal cone GA
- `GA_OnsetShadowstep.h/.cpp` — passive on-kill teleport GA
- `OnsetStateTreeAoETask.h/.cpp` — NPC task for AoE (if NPCs use it)
- `OnsetStateTreeConeTask.h/.cpp` — NPC task for cone (if NPCs use it)

### BP assets (Content/Game/Combat/)
- `GA_AoE` (BP child of C++ — tune radius, damage, cooldown)
- `GA_Cone` (BP child — tune angle, range, damage, cooldown)
- `GA_Shadowstep` (BP child — tune distance gate, CD, teleport offset)

### Player integration
- Wire `OnAbility1` → `GA_AoE`, `OnAbility2` → `GA_Cone`
- Shadowstep triggers via `FGameplayEvent` on enemy death

### Episode 21 — Architecture Cleanup (completed)
- [x] **UInteractionComponent** — extracted `OnPrimaryInteraction` logic from `AOnsetPlayerController`
- [x] Component owned by controller, owner resolved via `Cast<AOnsetPlayerController>(GetOwner())`
- [x] TargetingComponent resolved lazily via `PlayerController->GetPawn()`
- [x] PvP check uses `PlayerController->GetPawn()` (matches original behaviour)
- [x] **Profile Split** — `UAIProfile` split into 3 focused data assets:
  - `UVisualProfile`: SkeletalMesh, CorpseMesh, AnimBlueprintClass, OverrideMaterial
  - `UAIProfile`: StateTreeAsset, Aggression, FleeThreshold, AssistRadius, AttackRange, ChaseRange
  - `UPerceptionProfile`: SightRange, SightAngle, HearingRange
- [x] Moved to `Enemy/Profile/` directory, old `AI/AIProfile.h` removed
- [x] **Call sites updated:**
  - `AOnsetEnemy` — holds all 3 profile refs, `ApplyProfile` takes `UVisualProfile*`
  - `AOnsetAIController` — `ApplyAIProfile(UAIProfile*)` + `ApplyPerceptionProfile(UPerceptionProfile*)`
  - `FSpawnConfig` — `EnemyAIProfile`, `EnemyVisualProfile`, `EnemyPerceptionProfile`
  - `AOnsetSpawner` — passes all 3 profiles on spawn
  - `OnsetStateTreeDistanceCondition` — `Self->Profile->AttackRange/ChaseRange` (UAIProfile)
  - `OnsetStateTreeFleeCondition` — `Self->Profile->FleeThreshold` (UAIProfile)
- [x] `OnsetPoolSubsystem` — `ApplyProfile(nullptr)` maps to `UVisualProfile*` nullptr

## What's Next
- A4.6 implementation (AoE, Cone, Shadowstep)
- A4.2 Animation montage (simple melee swing)
- Episode 22 — GAS Movement Speed Attribute
