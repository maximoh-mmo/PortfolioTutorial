# 🗓 THU 18 JUN — Sprint Day 6: Player AI Autoplay (Part 1)

## 1. PlayerAIController Class
- [ ] Create `Public/AI/PlayerAIController.h` — inherits `AAIController`
- [ ] Create `Private/AI/PlayerAIController.cpp`
- [ ] Owns `UStateTreeAIComponent*` (default subobject)
- [ ] `OnPossess(APawn*)` — cache `TargetingComponent` from pawn, start StateTree logic
- [ ] `OnUnPossess()` — stop StateTree, clear cache, hide/disable

## 2. EnableAutoplay / DisableAutoplay
- [ ] `EnableAutoplay(AOnsetPlayerController* PlayerOwner)` — cache `PlayerOwner`, call `PlayerOwner->UnPossess()`, then `Possess(Pawn)`
- [ ] `DisableAutoplay()` — `UnPossess()` (stops tree, clears cache), then `PlayerOwner->Possess(Pawn)` (restores input)
- [ ] Verify swap is clean — no movement stutter, no lost targeting

## 3. Player AI StateTree Schema
- [ ] Create `Public/AI/PlayerAIStateTreeSchema.h/.cpp`
- [ ] Context data: `AOnsetBaseCharacter* SelfActor`, `AActor* TargetActor`
- [ ] Register schema class

## ✅ Done
- [ ] (filled in after work)

## What's Next
- Player AI tasks (FindTarget, MoveTo, Attack)
- Player AI StateTree BP asset
