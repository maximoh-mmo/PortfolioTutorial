# 🗓 WED 10 JUN — A2.2 Object Pooling (Day 2)

## 1. Implement ResetNPC()
- [ ] Clear health attributes (set to max)
- [ ] Clear AI state (stop any active behaviours, clear target)
- [ ] Reset visuals (mesh, material overrides, scale)
- [ ] Clear group membership data
- [ ] Reset any timers or delegates

## 2. Integrate Spawner → PoolManager Flow
- [ ] `AEnemySpawner` references `ANPCPoolManager` (via world or spawner property)
- [ ] `SpawnGroup()` calls `PoolManager->GetNPC()` instead of `SpawnActor`
- [ ] `DestroyGroup()` calls `PoolManager->ReleaseNPC()` instead of Destroy

## 3. Edge Case: Pool Exhaustion
- [ ] Test with small pool + large group → fallback `SpawnActor` creates new NPCs
- [ ] Verify exhausted NPCs are also releaseable back to "pool" (add to array even if not pre-allocated)
- [ ] No crash when pool runs out

## 4. Test
- [ ] Compile → Play
- [ ] Spawner uses pooled NPCs (check via log or breakpoint)
- [ ] NPCs reset correctly on reuse (position, health, tags)
- [ ] No stale group or target data after reset
- [ ] No crash when pool is exhausted

## ✅ Done
- [ ] Check off **A2.2** in `TODO/Private_Demo_Checklist.md`
