# 🗓 THU 9 JUL — A5.4 Steam Auth: Server Validation

## Day 9 — Server-Side Ticket Validation
- [ ] Add Server_SendAuthTicket(const TArray<uint8>& AuthTicket) reliable RPC to PlayerController
- [ ] Add ValidateAuthTicket(APlayerController*, const TArray<uint8>&) to GameMode
- [ ] Server-side: parse ticket via GetIdentityInterface()->GetAuthTicketHandle()
- [ ] Implement ticket expiry/timeout: 10s timer on client
- [ ] Handle invalid ticket: reject client with log + disconnect message
- [ ] Handle expired ticket: allow retry (up to 3 attempts)
- [ ] Handle timeout: display error, retry or disconnect

## Verify
- [ ] Server_SendAuthTicket RPC compiles
- [ ] Server receives and processes ticket
