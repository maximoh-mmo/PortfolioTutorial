# 🗓 THU 11 JUN — A2.3 Group System (Day 1)

## 1. Create FGroupData Struct (BlueprintType)
- [x] `FGroupData` defined in `Source/Onset/Public/Enemy/GroupComponent.h`
- [x] `USTRUCT(BlueprintType)` Fields: `FVector Center`, `int32 AliveCount`, `float AssistRadius`
> **Note:** Moved alongside `UGroupComponent` instead of separate file. `FVector Direction` deferred to A3.3 Roam.

## 2. Create UGroupManagerComponent (component, not actor)
- [x] `Source/Onset/Public/Spawning/GroupManagerComponent.h` — `UCLASS()`, inherit `UActorComponent`
- [x] Hosted on `AOnsetSpawner` instead of standalone actor
- [x] `RegisterMember(AOnsetEnemy*)` — takes pawn directly, no `int32 GroupID`
- [x] `UnregisterMember(AOnsetEnemy*)`
> **Design change:** Evolved from standalone `AGroupManager` actor to `UGroupManagerComponent` on the spawner — simpler lifecycle, no singleton, no world lookup.

## 3. Create UGroupComponent (Blueprintable + BlueprintSpawnableComponent)
- [x] `Source/Onset/Public/Enemy/GroupComponent.h` — `UCLASS(Blueprintable, BlueprintType, meta=(BlueprintSpawnableComponent))`, inherit `UActorComponent`
- [x] Reference to `UGroupManagerComponent` (set on registration)
- [x] `UnregisterFromGroup()` convenience method
- [x] Attached to `AOnsetEnemy` as a default subobject

## 4. Implement RegisterMember / UnregisterMember
- [x] On register: add NPC to member list
- [x] On unregister: remove NPC from member list, set component's manager ref to null
- [x] If group empty: no special cleanup needed (members array empties)

## 5. Integrate Spawner → GroupManager
- [x] `AOnsetSpawner` hosts `UGroupManagerComponent` as subobject
- [x] After spawning each NPC: calls `GroupManager->RegisterMember(NPC)`

## 6. Test
- [x] Compile → Play
- [x] NPCs register correctly
- [x] GroupManager tracks members
- [x] DestroyGroup / pool return → UnregisterMember works

## ✅ Done
- [x] Track progress toward **A2.3** in `TODO/Private_Demo_Checklist.md`
