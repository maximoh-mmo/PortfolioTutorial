# 🗓 TUE 2 JUN — A1.5 Ability Targeting System

## Design
- Static library `UAbilityTargetingLibrary` + `FAbilityTargetData` struct
- No component, no PlayerController changes — abilities call the library directly
- `GetTargetData(TargetingComponent, SourceActor)` returns actor, location, direction from current target
- `FAbilityTargetData` stripped: no Radius (set per-ability), no PlayerPawn-typed fields (generic SourceActor)

## 1. Create FAbilityTargetData Struct + UAbilityTargetingLibrary
- [x] `Source/Onset/Public/Combat/AbilityTargetingLibrary.h` — struct + static method
- [x] `Source/Onset/Private/Combat/AbilityTargetingLibrary.cpp` — implement
- [x] `FAbilityTargetData` fields: `TargetActor`, `TargetLocation`, `TargetDirection`
- [x] `UAbilityTargetingLibrary::GetTargetData(TargetingComponent*, AActor* SourceActor)` — populate data from current target, compute direction

## 2. Update Ability Stubs
- [x] In `OnsetPlayerController.cpp`, update `OnAbility1-4()` to call `UAbilityTargetingLibrary::GetTargetData()` and log

## 3. Test
- [x] Compile → Play
- [x] Click enemy → target set → ability key prints target actor name + location
- [x] Click ground → target cleared → ability key prints "no target"

## ✅ Done
- [x] Check off **A1.5** in `TODO/Private_Demo_Checklist.md`
