# 🗓 THU 28 MAY — A1.2 Top-Down Camera

## 1. Add Camera Components to Header
- [x] Open `AOnsetPlayerCharacter.h`
- [x] Add forward declarations: `class USpringArmComponent;`, `class UCameraComponent;`
- [x] Add `UPROPERTY()` for `USpringArmComponent* CameraBoom`
- [x] Add `UPROPERTY()` for `UCameraComponent* FollowCamera`

## 2. Create Components in Constructor
- [x] Open `AOnsetPlayerCharacter.cpp`
- [x] Add includes: `SpringArmComponent.h`, `CameraComponent.h`
- [x] `CameraBoom = CreateDefaultSubobject<USpringArmComponent>(TEXT("CameraBoom"))`
- [x] `CameraBoom->SetupAttachment(RootComponent)`
- [x] `CameraBoom->TargetArmLength = 1000.0f`
- [x] `CameraBoom->SetRelativeRotation(FRotator(-60.0f, 0.0f, 0.0f))`
- [x] `CameraBoom->bDoCollisionTest = true`
- [x] `CameraBoom->bEnableCameraLag = true`
- [x] `CameraBoom->CameraLagSpeed = 8.0f`
- [x] `FollowCamera = CreateDefaultSubobject<UCameraComponent>(TEXT("FollowCamera"))`
- [x] `FollowCamera->SetupAttachment(CameraBoom, USpringArmComponent::SocketName)`

## 3. Set Default Pawn
- [x] In `AOnsetGameMode` constructor, set `DefaultPawnClass = AOnsetPlayerCharacter::StaticClass()`

## 4. Test
- [x] Compile → Play
- [x] Camera follows character from top-down angle
- [x] Movement feels smooth (camera lag working)
- [x] Walk near walls → camera pushes back instead of clipping
