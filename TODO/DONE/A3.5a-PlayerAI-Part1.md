# 🗓 THU 18 JUN — Sprint Day 6: Player AI Autoplay (Part 1) ✅

## 1. PlayerAIController Class
- [x] Create `Public/AI/PlayerAIController.h` — inherits `AAIController` (created as `AOnsetPlayerAIController`)
- [x] Create `Private/AI/PlayerAIController.cpp`
- [x] Owns `UStateTreeAIComponent*` (default subobject)
- [x] `OnPossess(APawn*)` — cache `TargetingComponent` from pawn, start StateTree logic
- [x] `OnUnPossess()` — stop StateTree, clear cache, hide/disable

## 2. EnableAutoplay / DisableAutoplay
- [x] `EnableAutoCombat()` — cache `PlayerOwner`, call `PlayerOwner->UnPossess()`, then `Possess(Pawn)`
- [x] `DisableAutoCombat()` — `UnPossess()` (stops tree, clears cache), then `PlayerOwner->Possess(Pawn)` (restores input)
- [x] Verify swap is clean — camera handover needed `DelayedSetViewTarget` fix

## 3. Player AI StateTree Schema
- [x] Created `UOnsetStateTreeSchema` (shared with NPCs, not separate schema)
- [x] Context data: `AOnsetBaseCharacter* SelfActor`, `AActor* TargetActor`
- [x] Register schema class

## Notes
- Camera handover required `DelayedSetViewTarget` — one-frame defer avoids race with `PlayerCameraManager`
- Same `UOnsetStateTreeSchema` used for both NPC and Player StateTrees
- `GetTargetingComponent()` fast-path: NPC cast ~3ns, Player fallback ~20ns
