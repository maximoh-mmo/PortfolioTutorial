# 🗓 MON 8 JUN — A2.1 NPC Character + Spawner (Day 2)

## 1. Implement SpawnGroup()
- [x] `AOnsetSpawner::SpawnGroup()` — loops all slots, fills empty ones via `SpawnEnemyAtSlot()`
- [x] Slot transforms from `SpawnPoints` or fallback ring scatter
- [x] Collision handling: `ESpawnActorCollisionHandlingMethod::AdjustIfPossibleButAlwaysSpawn` (in pool's spawn, not spawner — spawner always uses pool)
> **Evolution:** Now uses slot-based spawning with `FSpawnerSlot` struct instead of inline loop. Pool integration replaced direct `SpawnActor`.

## 2. Implement InitSlots()
- [x] `InitSlots()` — pre-computes slot transforms from `SpawnPoints` or fallback ring scatter
- [x] `SpawnPoints[i]` used if valid index
- [x] Fallback: circular distribution using `SpawnConfig.SpawnRadius`

## 3. Implement DestroyGroup()
- [x] Slot-based: iterate `Slots` array, destroy each occupant via pool's `ReleasePooledEnemy`
- [x] Clear slot occupant reference

## 4. Level Setup + Auto-Spawn on BeginPlay
- [x] `SpawnGroup()` called in `BeginPlay()` for testing
- [x] Place spawner in level with `UAIProfile` assigned
- [x] NPCs appear with `"Enemy"` tag

## 5. Test
- [x] Compile → Play
- [x] Spawner creates correct number of NPCs
- [x] Spawn points override scatter
- [x] DestroyGroup() cleans up via pool
- [x] NPCs have `"Enemy"` tag

## ✅ Done
- [x] Check off **A2.1** in `TODO/Private_Demo_Checklist.md`
