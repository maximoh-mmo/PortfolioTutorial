# 🗓 MON 27 JUL — Packaging Fix, CommonUI Verified, Movement Diagnosis

## Summary
Diagnosed and fixed stale Shipping binary issue that prevented CommonUI screens from appearing in packaged builds. New screens (`WBP_RootLayout`, `WBP_MainMenu`, `WBP_CharacterSelect`) now render correctly. Movement and nav mesh issues remain in DemoLevel.

## Packaging Fix
- **Root cause identified:** `Package/Windows/Onset/Binaries/Win64/Onset.exe` was a Shipping build from **30 Jun 2026** — before the CommonUI migration commits on 8 Jul. The old binary lacked `UOnsetUISubsystem`, `StartPlay()` UI push, and `CommonUI` module dependency.
- **New Blueprints missing from cook:** `WBP_RootLayout`, `WBP_MainMenu`, `WBP_CharacterSelect` were not in `Manifest_UFSFiles_Win64.txt` because the cooker saw no C++ references (old CDO).
- **`LaunchServerAndClient.ps1`** pointed at stale `OnsetGameMode` (was `OnsetGameModeBase` after rename) → server fell back to old `OnsetGameMode_C` Blueprint.
- **Fix:** Clean rebuild of all Binaries, Intermediate, and Saved folders → repackaged → Shipping binary now from 27 Jul 2026 with new code.
- **Verified:** `Game class is 'OnsetMenuGameMode'` in client log, no CDO errors.

## Remaining Issues
- **Nav mesh:** DemoLevel needs Navigation Data rebuilt in Editor.
- **Character doesn't move:** Suspect Input Mapping Contexts / `IA_Move` not assigned in `MyOnsetPlayerController` Blueprint CDO, or input mode blocking game input after CommonUI flow. Requires Editor investigation.

## Key Decisions
- All CommonUI migration tasks (A5b.5) confirmed working in packaged build.
- Sprint A5c (Auth Extraction) remains not started — packaging and movement issues are blocking.
- Next session: fix nav mesh + movement in Editor, then proceed to A5c.
