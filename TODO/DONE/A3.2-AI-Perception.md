# 🗓 TUE 2 JUN — A3.2 AI Perception

## 1. Implement OnPerceptionUpdated()
- [x] In `AOnsetAIController.cpp` — fill the empty `OnPerceptionUpdated()` body
- [x] On sight: if perceived `AActor*` is a valid player → set TargetingComponent target
- [x] On hearing: if noise source detected → add to perceived actors, filtered same as sight
- [x] Handle stimulus expiry (no perceived actors → ClearTarget)

## 2. Feed Perception into StateTree Context
- [x] Wire `OnPerceptionUpdated` results into the StateTree context via TargetingComponent (`FOnsetStateTreeContextTask` reads TargetingComponent for target actor)
- [x] Ensure perception data is available to StateTree evaluators/conditions (currently task-based; evaluator path TBD)

## 3. Verify
- [x] Compile → Play
- [x] Player walks near NPC → perception triggers → Agro fires
- [x] Player leaves sight → Lost fires
- [x] (Noise-based assist tested in A3.4 — deferred until damage noise events exist)

## ✅ Done
- [x] Sight-based perception fully working. Hearing integration deferred to A3.4/ A4.
- [x] Check off **A3.2** in `TODO/Private_Demo_Checklist.md`
