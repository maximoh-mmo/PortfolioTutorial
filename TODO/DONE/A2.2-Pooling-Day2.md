# 🗓 WED 10 JUN — A2.2 Object Pooling (Day 2)

## 1. Implement ResetNPC() → ApplyProfile(nullptr)
- [x] Clear health attributes — handled by profile reset (health deferred to A4 GAS)
- [x] Clear AI state — `ApplyProfile(nullptr)` clears anim BP; controller reset handled on next possess
- [x] Reset visuals — `ApplyProfile(nullptr)` clears skeletal mesh, material, destroys fallback cube
- [x] Clear group membership — `UnregisterFromGroup()` called in `ReturnToPool()`
- [x] Reset location, collision, tick, input — all done in `ReturnToPool()`
> **Note:** Implemented as `ReturnToPool()` → `ApplyProfile(nullptr)` rather than a single `ResetNPC()` function.

## 2. Integrate Spawner → PoolManager Flow
- [x] `AOnsetSpawner` has `PoolManager` property (TObjectPtr)
- [x] `SpawnEnemyAtSlot()` calls `PoolManager->GetPooledEnemy()` instead of `SpawnActor`
- [x] `ReleasePooledEnemy(Enemy)` used instead of `Destroy` on death

## 3. Edge Case: Pool Exhaustion
- [x] `GetPooledEnemy()` fallback: `SpawnActor<AOnsetEnemy>` when all pooled NPCs are in use
- [x] Exhausted NPCs added to pool array, releaseable normally
- [x] No crash when pool runs out

## 4. Test
- [x] Compile → Play
- [x] Spawner uses pooled NPCs
- [x] NPCs reset correctly on reuse
- [x] No stale group or target data after reset
- [x] No crash when pool is exhausted

## ✅ Done
- [x] Check off **A2.2** in `TODO/Private_Demo_Checklist.md`
