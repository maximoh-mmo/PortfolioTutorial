# 🗓 FRI 5 JUN — A2.1 NPC Character + Spawner (Day 1)

## 1. Create AOnsetEnemy (BlueprIntable handled via BP reparent)
- [x] `Source/Onset/AI/OnsetEnemy.h` — `UCLASS()`, inherits `AOnsetBaseCharacter`, `GENERATED_BODY()`
- [x] `Source/Onset/AI/OnsetEnemy.cpp` — default constructor adds `"Enemy"` tag, sets `AIControllerClass`
- [x] Mesh + collision provided by existing `AOnsetBaseCharacter` + reparented BP

## 2. Create AOnsetAIController (stub)
- [x] `Source/Onset/AI/OnsetAIController.h` — `UCLASS()`, inherits `ADetourCrowdAIController` (from AI Crowd plugin)
- [x] `Source/Onset/AI/OnsetAIController.cpp` — empty stub

## 3. Create FSpawnConfig Struct (BlueprintType)
- [x] `Source/Onset/Spawning/SpawnConfig.h`
- [x] `USTRUCT(BlueprintType)` Fields: `TSubclassOf<AOnsetEnemy> EnemyClass`, `int32 GroupSize`, `float SpawnRadius`, `float RespawnDelay`

## 4. Create AOnsetSpawner (Blueprintable)
- [x] `Source/Onset/Spawning/OnsetSpawner.h` — `UCLASS(Blueprintable)`, inherits `AActor`
- [x] `UPROPERTY` `FSpawnConfig Config`
- [x] `UPROPERTY` `TArray<AActor*> SpawnPoints`
- [x] Declare `SpawnGroup()`, `DestroyGroup()`, `GetSpawnLocation(int32 Index)`, `SpawnSingleNPC()`
- [x] `.cpp` — implementation: ring-based scatter fallback, point-based when points set

## 5. Test
- [x] Compile — no errors
- [x] Place spawner in level with `BP_OnsetEnemy` as EnemyClass
- [x] Press Play — spawner creates group, `DestroyGroup()` clears them
- [x] Verify config `GroupSize` / `SpawnRadius` respected

## ✅ Done
- [x] Track progress toward **A2.1** in `TODO/Private_Demo_Checklist.md`
