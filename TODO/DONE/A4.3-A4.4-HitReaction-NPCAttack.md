# 🗓 SUN 14 JUN — A4.3 Hit Reaction + A4.4 NPC Attack Integration

## ✅ Done

### A4.3 Hit Reaction
- [x] `UOnsetGA_HitReaction` — `InstancedPerActor`, triggered by `TAG_Event_HitReaction` via `FAbilityTriggerData` (GameplayEvent source)
- [x] `PostGameplayEffectExecute` fires `HandleGameplayEvent(TAG_Event_HitReaction, Payload)` on damage (Magnitude < 0)
- [x] Payload includes `Instigator`, `Target`, `EventMagnitude`
- [x] BP assets: `GE_HitReactionCooldown` (0.5s, grants `Cooldown.HitReaction`), `GA_HitReaction` (BP child, CooldownGE set)
- [x] `GrantDefaultAbilities()` grants `GA_HitReaction` to all base characters
- [x] Hit reaction verified — plays on damage
- [x] Cooldown verified — prevents hit-reaction spam

### A4.4 NPC Attack Integration
- [x] `FOnsetStateTreeAttackTask` — `TSubclassOf<UGameplayAbility> AbilityClass` property
- [x] `EnterState` → `StopMovement()`. `TickState` → cooldown tag check (`Running` if present) → `TryActivateAbilityByClass`
- [x] GAS GE owns cooldown rhythm (`GE_BasicAttackCooldown`), no StateTree timer
- [x] NPC attacks player in range — verified
- [x] NPC exits Attack state on cooldown — verified

### Fixes
- [x] `CooldownGameplayEffectClass` type fix (UE5.8 API — `const UGameplayEffect*`, not `TSubclassOf`)
- [x] `FOnsetStateTreeAttackTask` migrated to `FOnsetStateTreeTaskBase`

## What's Next
- A4.5a Death → Pool Return → Respawn
- A2 animation montage (deferred — still using primitive objects)
- A4.3 stagger/hitstop effect
