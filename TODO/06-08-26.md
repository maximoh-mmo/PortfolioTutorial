# 🗓 MON 8 JUN — A2.1 NPC Character + Spawner (Day 2)

## 1. Implement SpawnGroup()
- [ ] In `AEnemySpawner::SpawnGroup()`:
  - Loop `SpawnConfig.GroupSize` times
  - Call `GetSpawnLocation(i)` for each
  - `GetWorld()->SpawnActor<ANPCCharacter>(SpawnConfig.EnemyClass, Location, Rotation, Params)`
- [ ] Collision handling: `ESpawnActorCollisionHandlingMethod::AdjustIfPossibleButAlwaysSpawn`

## 2. Implement GetSpawnLocation()
- [ ] If `SpawnPoints` has valid index → use point location
- [ ] Fallback: scatter around spawner center using circular distribution + `SpawnRadius`

## 3. Implement DestroyGroup()
- [ ] Track spawned NPCs in `TArray<AActor*> SpawnedGroup`
- [ ] `DestroyGroup()` → iterate and destroy each, clear array

## 4. Level Setup + Auto-Spawn on BeginPlay
- [ ] Call `SpawnGroup()` in `BeginPlay()` for testing
- [ ] Place spawner in level with enemy class assigned
- [ ] Verify NPCs appear with correct tags and collision

## 5. Test
- [ ] Compile → Play
- [ ] Spawner creates correct number of NPCs
- [ ] Spawn points override scatter
- [ ] DestroyGroup() cleans up all spawned NPCs
- [ ] NPCs have `"Enemy"` tag (required for targeting from E4)

## ✅ Done
- [ ] Check off **A2.1** in `TODO/Private_Demo_Checklist.md`
