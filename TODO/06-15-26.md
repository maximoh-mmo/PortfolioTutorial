# 🗓 MON 15 JUN — A4.5a Death, Pool Return, Respawn + CheatManager

## ✅ Done

### Death Detection
- [x] `PostGameplayEffectExecute` computes `OldHealth = GetHealth() - Magnitude` (true pre-damage value), clamps, detects death when `Health ≤ 0 && OldHealth > 0`
- [x] Fires `HandleGameplayEvent(TAG_Event_Death, Payload)` for future corpse system (A4.5b)
- [x] Direct `Character->OnDeath(Instigator)` call for immediate death actions
- [x] Bug fixed: god mode guard moved outside death check block — now reverts ALL damage, not just lethal blows

### Base Character Death
- [x] `AOnsetBaseCharacter::OnDeath()` — virtual, `checkf` crasher warns subclasses must override
- [x] `HomeLocation` (FVector) → `HomeTransform` (FTransform): stores rotation
- [x] Updated all references: Spawner, DistanceCondition, RoamTask, PlayerCharacter

### NPC Death → Pool Return
- [x] `AOnsetEnemy::OnDeath()` — defer to next tick via `SetTimerForNextTick`
- [x] `DeferredDeathCleanup()` — notify `OwningSpawner->OnNPCDeath(this)`
- [x] `AOnsetEnemy::OwningSpawner` set in `SpawnEnemyAtSlot`, cleared in `ReturnToPool`

### Spawner Respawn
- [x] `AOnsetSpawner::OnNPCDeath()` — find slot by occupant, clear, start per-slot respawn timer (`Config.RespawnDelay`), release to pool
- [x] `AOnsetSpawner::RespawnNPC()` — invalidate timer, call `SpawnEnemyAtSlot`
- [x] `FSpawnerSlot::RespawnTimerHandle` — per-slot timer for independent respawns

### Player Respawn
- [x] `AOnsetPlayerCharacter::OnDeath()` — `DisableInput(nullptr)`, defer `RespawnPlayer()`
- [x] `RespawnPlayer()` — `SetHealth(MaxHealth)`, `SetActorTransform(HomeTransform)`, `EnableInput(PC)`
- [x] `BeginPlay()` — saves `HomeTransform = FTransform(Rotation, Location)`

### Controller Pooling
- [x] `AOnsetAIController::OnUnPossess()` — now does full reset: stop StateTree, `ApplyProfile(nullptr)`, hide, disable tick/collision/input
- [x] `ReleasePooledController()` calls `UnPossess()` directly — `ResetForPool` removed (dead code)
- [x] `ReturnToPool` clears `OwningSpawner = nullptr` on pawn

### CheatManager (UOnsetCheatManager)
- [x] `God()` — toggles `TAG_State_Invulnerable` tag via `AddLooseGameplayTag`/`RemoveLooseGameplayTag`
- [x] God mode guard in `PostGameplayEffectExecute` — reverts all damage when tag present, returns early
- [x] `Heal()` — sets player `Health = MaxHealth` via `AttributeSet->SetHealth`
- [x] `CheatClass = UOnsetCheatManager::StaticClass()` in PlayerController constructor
- [x] Accessible via console (~): `God` toggle, `Heal` full restore

## Verified
- [ ] NPC death → pool return → respawn timer → re-spawn (confirmed working)
- [ ] Multiple simultaneous NPC deaths — independent per-slot timers
- [ ] Player death → full heal → teleport to HomeTransform → controls re-enabled
- [ ] God mode blocks all damage (not just lethal)
- [ ] Heal restores health to max
- [ ] Pool recycles NPCs and controllers without leaks

## What's Next
- A4.5b — Corpse Actor System (design doc in `Docs/AI/Corpse_System.md`)
- A4.3 — stagger/hitstop effect
- A4.2 — animation montage (deferred, still using primitives)
- A3.3 — Flee state (low health threshold)
- A3.4 — Group Assist (damage noise events)
- A3.5 — Player AI Autoplay
