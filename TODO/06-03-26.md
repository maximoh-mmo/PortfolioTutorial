# 🗓 WED 3 JUN — A1.6 PvP Toggle

## 1. Add PvP Flag to PlayerState
- [x] In `AOnsetPlayerState.h`: `UPROPERTY(ReplicatedUsing = OnRep_PvPEnabled)` `bool bIsPvPEnabled`
- [x] Declare `UFUNCTION()` `OnRep_PvPEnabled()`
- [x] In `.cpp`: implement `OnRep_PvPEnabled()`, add `DOREPLIFETIME(AOnsetPlayerState, bIsPvPEnabled)`

## 2. Add Server RPC
- [x] In `AOnsetPlayerController.h`:
  - `UFUNCTION(Server, Reliable)` `Server_SetPvPEnabled(bool bEnabled)`
  - `OnPvPToggleTriggered()` — bound to `IA_PvPToggle`
- [x] In `.cpp`:
  - `Server_SetPvPEnabled`: set `GetPlayerState<AOnsetPlayerState>()->bIsPvPEnabled = bEnabled`
  - `OnPvPToggleTriggered`: toggle local state, call `Server_SetPvPEnabled`

## 3. Bind IA_PvPToggle
- [x] Bind `IA_PvPToggle` in `SetupInputComponent()` → `OnPvPToggleTriggered`
- [x] P key (desktop), virtual button (touch), D-pad down (gamepad)

## 4. Add PvP Filtering to TargetingComponent
- [x] Add `bool IsActorTargetPVPValid(AActor* Target, AActor* SourcePlayer)` to `UTargetingComponent`
- [x] Implement: if Target is a player character, check `bIsPvPEnabled` on source's PlayerState
- [x] Call `IsActorTargetPVPValid()` inside `OnPrimaryInteraction()` before assigning

## 5. Test
- [x] Compile
- [x] Run PIE with 2 players (Advanced → Number of Players: 2)
- [x] Press P → toggle ON → can target other player
- [x] Press P → toggle OFF → cannot target other player
- [x] PvP OFF while targeting a player → target auto-clears
- [x] PvP toggle accessible via touch (virtual button) and gamepad (D-pad down)

## ✅ Done
- [x] Check off **A1.6** in `TODO/Private_Demo_Checklist.md`
