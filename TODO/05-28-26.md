# 🗓 THU 28 MAY — A1.2 Top-Down Camera

## 1. Add Camera Components to Header
- [ ] Open `AOnsetCharacter.h`
- [ ] Add forward declarations: `class USpringArmComponent;`, `class UCameraComponent;`
- [ ] Add `UPROPERTY()` for `USpringArmComponent* CameraBoom`
- [ ] Add `UPROPERTY()` for `UCameraComponent* FollowCamera`

## 2. Create Components in Constructor
- [ ] Open `AOnsetCharacter.cpp`
- [ ] Add includes: `SpringArmComponent.h`, `CameraComponent.h`
- [ ] `CameraBoom = CreateDefaultSubobject<USpringArmComponent>(TEXT("CameraBoom"))`
- [ ] `CameraBoom->SetupAttachment(RootComponent)`
- [ ] `CameraBoom->TargetArmLength = 1000.0f`
- [ ] `CameraBoom->SetRelativeRotation(FRotator(-60.0f, 0.0f, 0.0f))`
- [ ] `CameraBoom->bDoCollisionTest = true`
- [ ] `CameraBoom->bEnableCameraLag = true`
- [ ] `CameraBoom->CameraLagSpeed = 8.0f`
- [ ] `FollowCamera = CreateDefaultSubobject<UCameraComponent>(TEXT("FollowCamera"))`
- [ ] `FollowCamera->SetupAttachment(CameraBoom, USpringArmComponent::SocketName)`

## 3. Set Default Pawn
- [ ] In `AOnsetGameMode` constructor, set `DefaultPawnClass = AOnsetCharacter::StaticClass()`

## 4. Test
- [ ] Compile → Play
- [ ] Camera follows character from top-down angle
- [ ] Movement feels smooth (camera lag working)
- [ ] Walk near walls → camera pushes back instead of clipping

## ✅ Done
- [ ] Check off **A1.2** in `TODO/Private_Demo_Checklist.md`
