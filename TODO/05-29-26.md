# 🗓 FRI 29 MAY — A1.3 Movement System (Joystick + Tap + WASD + Gamepad)

## 1. Enable Mouse Cursor & Touch
- [ ] Override `BeginPlay()` in `AOnsetPlayerController`
- [ ] `bShowMouseCursor = true` + `DefaultMouseCursor = EMouseCursor::Default`
- [ ] Enable touch input in Project Settings

## 2. Virtual Joystick Widget (Touch)
- [ ] Create `UJoystickWidget` (UserWidget) with touch zone + thumb visual
- [ ] Implement touch begin/move/end → normalized 2D axis
- [ ] Feed axis values into `IA_Move` via `UEnhancedInputLocalPlayerSubsystem::InjectInputForAction()`

## 3. IA_Confirm → Raycast → MoveToLocation (Tap + Click)
- [ ] In `AOnsetPlayerController`:
  - `SetupInputComponent()` → `UEnhancedInputComponent->BindAction(IA_Confirm, ...)`
  - Handler: `GetHitResultUnderCursor(ECC_Visibility, Hit)` → `MoveToLocation(Hit.Location)`
- [ ] Works for both mouse click and touch tap via cursor abstraction

## 4. IA_Move → Direct Movement (Joystick + WASD + L-Stick)
- [ ] In `AOnsetCharacter`:
  - `SetupPlayerInputComponent()` → `UEnhancedInputComponent->BindAction(IA_Move, ...)`
  - Handler: apply `EnhancedInputInputActionValue.Get<FVector2D>()` to character movement
- [ ] On any `IA_Move` input: call `StopMovement()` to interrupt active pathfinding
- [ ] When `IA_Move` returns to zero: resume pathfinding if there's a pending destination

## 5. Gamepad R-Stick Cursor
- [ ] In `AOnsetPlayerController`:
  - `BindAction(IA_Cursor, ...)` — accumulate axis delta onto cursor position
  - Clamp to viewport bounds
- [ ] Create `UGamepadCursorWidget` (simple crosshair reticle)
- [ ] Show/hide cursor widget when R-Stick moves / idle

## 6. Cursor Abstraction Layer
- [ ] `UCursorManager::GetCursorPosition(FVector2D& OutScreenPos)`:
  - Mouse path: `PlayerController->GetMousePosition()`
  - Touch path: last touch event screen location
  - Gamepad path: accumulated R-Stick position
- [ ] Replace all raw `GetHitResultUnderCursor` calls with `CursorManager->GetCursorPosition()` → `PlayerController->GetHitResultAtScreenPosition()`

## 7. Nav Mesh
- [ ] Place a **Nav Mesh Bounds Volume** in the level covering the playable area

## 8. Test
- [ ] Compile → Play
- [ ] Tap ground (touch) → character moves to location
- [ ] Virtual joystick (touch) → character moves continuously
- [ ] Click ground (mouse) → character moves to location
- [ ] WASD (keyboard) → character moves with hand-off from pathfinding
- [ ] L-Stick (gamepad) → character moves
- [ ] R-Stick (gamepad) → cursor moves on screen, clamped to viewport
- [ ] Switch between input methods mid-movement
- [ ] Add obstacles → character navigates around

## ✅ Done
- [ ] Check off **A1.3** in `TODO/Private_Demo_Checklist.md`
