# 🗓 FRI 29 MAY — A1.3 Click-to-Move (Mouse + Touch)

## 1. Enable Mouse Cursor
- [ ] Override `BeginPlay()` in `AOnsetPlayerController`
- [ ] `bShowMouseCursor = true` + `DefaultMouseCursor = EMouseCursor::Default`

## 2. Enable Touch Input in Project
- [ ] Project Settings → Input → Enable `bShowMouseCursor` for touch devices
- [ ] Ensure `Touch Interface` is enabled in Engine → Input (mobile preview)

## 3. Create Input Mapping
- [ ] Project Settings → Input → Action Mapping `ClickMove` → Left Mouse Button
- [ ] Add secondary binding for Touch (if needed — UE routes touch taps through same action)

## 4. Implement Tap/Click-to-Move
- [ ] In `AOnsetPlayerController.h`:
  - `virtual void SetupInputComponent() override;`
  - `UFUNCTION()` `void OnClickMove();`
- [ ] In `.cpp`:
  - `SetupInputComponent()` → `BindAction("ClickMove", IE_Pressed, this, &AOnsetPlayerController::OnClickMove)`
  - `OnClickMove()` → raycast via `GetHitResultUnderCursor(ECC_Visibility)`, `MoveToLocation(Hit.Location)`
- [ ] Note: UE's `GetHitResultUnderCursor` works identically for both mouse clicks and touch taps — no separate touch code needed

## 5. Nav Mesh
- [ ] Place a **Nav Mesh Bounds Volume** in the level covering the playable area

## 6. Test
- [ ] Compile → Play
- [ ] Click ground → character moves to clicked location
- [ ] Touch tap (or simulate with mouse in mobile viewport) → character moves
- [ ] Click multiple times → movement updates
- [ ] Add obstacles → character navigates around

## ✅ Done
- [ ] Check off **A1.3** in `TODO/Private_Demo_Checklist.md`
