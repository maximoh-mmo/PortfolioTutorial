# 🗓 WED 3 JUN — A1.6 PvP Toggle

## 1. Add PvP Flag to PlayerState
- [ ] In `AOnsetPlayerState.h`: `UPROPERTY(ReplicatedUsing = OnRep_PvPEnabled)` `bool bIsPvPEnabled`
- [ ] Declare `UFUNCTION()` `OnRep_PvPEnabled()`
- [ ] In `.cpp`: implement `OnRep_PvPEnabled()` (empty for now), add `DOREPLIFETIME(AOnsetPlayerState, bIsPvPEnabled)`

## 2. Add Server RPC
- [ ] In `AOnsetPlayerController.h`:
  - `UFUNCTION(Server, Reliable)` `Server_SetPvPEnabled(bool bEnabled)`
  - `UFUNCTION()` `OnPvPToggleClicked()`
- [ ] In `.cpp`:
  - `Server_SetPvPEnabled`: set `GetPlayerState<AOnsetPlayerState>()->bIsPvPEnabled = bEnabled`
  - `OnPvPToggleClicked`: toggle local state, call `Server_SetPvPEnabled`

## 3. Console Key for Testing
- [ ] Bind a key (e.g. `P`) in `SetupInputComponent()` → `OnPvPToggleClicked`

## 4. Add PvP Filtering to TargetingComponent
- [ ] Add `bool IsActorValidTarget(AActor* Target, AActor* SourcePlayer)` to `UTargetingComponent`
- [ ] Implement: if Target is a player character, check `bIsPvPEnabled` on source's PlayerState
- [ ] Call `IsActorValidTarget()` inside `SetCurrentTarget()` before assigning

## 5. Test
- [ ] Compile
- [ ] Run PIE with 2 players (Advanced → Number of Players: 2)
- [ ] Press P → toggle ON → can target other player
- [ ] Press P → toggle OFF → cannot target other player
- [ ] PvP OFF while targeting a player → target auto-clears
- [ ] PvP toggle accessible via touch (UI button will be built in A6; for now verify the binding works in mobile viewport)

## ✅ Done
- [ ] Check off **A1.6** in `TODO/Private_Demo_Checklist.md`
