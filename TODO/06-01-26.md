# 🗓 MON 1 JUN — A1.4 Targeting Component

## 1. Create UTargetingComponent (Blueprintable + BlueprintSpawnableComponent)
- [ ] `Source/Onset/Combat/UTargetingComponent.h` — `UCLASS(Blueprintable, BlueprintType, meta=(BlueprintSpawnableComponent))`, inherit `UActorComponent`, private `AActor* CurrentTarget`, public `GetCurrentTarget()`, `SetCurrentTarget()`, `ClearTarget()`
- [ ] `Source/Onset/Combat/UTargetingComponent.cpp` — implement the three methods

## 2. Add to PlayerController
- [ ] In `AOnsetPlayerController.h`: `UPROPERTY()` `UTargetingComponent* TargetingComponent`
- [ ] In constructor: `TargetingComponent = CreateDefaultSubobject<UTargetingComponent>(TEXT("TargetingComponent"))`

## 3. Modify IA_Confirm Handler for Targeting
- [ ] Replace simple click-to-move handler with branching logic:
  - Get cursor position from `UCursorManager`
  - Raycast
  - If actor has `"Enemy"` tag → `TargetingComponent->SetCurrentTarget(Hit.GetActor())`
  - Else → `MoveToLocation(Hit.Location)`

## 4. Stub an Enemy for Testing
- [ ] Create a temporary Blueprint or C++ actor with the `"Enemy"` tag, place in level

## 5. Create Basic Attack Input (IA_Target)
- [ ] Bind `IA_Target` in `SetupInputComponent()` → `OnBasicAttack()`
- [ ] `OnBasicAttack()` → if `CurrentTarget` exists, log its name via `UE_LOG`

## 6. Test
- [ ] Compile → Play
- [ ] Click/tap enemy → target is set
- [ ] Click/tap ground → character moves, target clears
- [ ] Press IA_Target key → target name appears in log
- [ ] Gamepad R-Stick cursor over enemy → A button to target → IA_Target bumper to attack

## ✅ Done
- [ ] Check off **A1.4** in `TODO/Private_Demo_Checklist.md`
