# 🗓 MON 1 JUN — A3.1 StateTree Schema

## 1. Create UNPCStateTreeSchema
- [x] Create `UNPCStateTreeSchema` — `UCLASS()`, inherits `UStateTreeSchema` with context data
- [x] Define context struct: self (FInstancedStruct), target (AActor*), group data (FGroupData), health (float)
- [ ] Expose context as evaluators for StateTree conditions

## 2. Bind NPC Context
- [x] Context data bound via `FOnsetStateTreeTaskBase` static helpers (replaced the Global Task approach — each task calls `GetSelfBaseCharacter()`, `GetController()`, `GetTarget()`)
- [x] Self → possessed pawn
- [x] Target → from TargetingComponent (set by `OnPerceptionUpdated`)
- [x] Group data → from UGroupComponent → GroupManager → GetGroupData()
- [x] Health → from GAS AttributeSet

## 3. Verify
- [x] Compile → Play
- [x] StateTree starts on NPC spawn (pool lifecycle fixes enable this)
- [ ] Debug display shows current AI state
- [ ] Add on-screen debug widget (or log) for state transitions

## ✅ Done
- [x] Schema + context binding + runtime verified. Debug display deferred.
- [x] Check off **A3.1** in `TODO/Private_Demo_Checklist.md`
