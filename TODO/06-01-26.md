# 🗓 MON 1 JUN — A1.4 Targeting Component

## 1. Create UTargetingComponent (data holder + validation, no Tick)
- [x] `Source/Onset/Public/Player/TargetingComponent.h` — `UCLASS(Blueprintable, BlueprintType, meta=(BlueprintSpawnableComponent))`, inherit `UActorComponent`, private `AActor* CurrentTarget`, public `GetCurrentTarget()`, `SetCurrentTarget()`, `ClearTarget()`, `IsActorValidTarget()`. `PrimaryComponentTick.bCanEverTick = false`.
- [x] `Source/Onset/Private/Player/TargetingComponent.cpp` — implement. No Tick override needed.

## 2. Add to PlayerController
- [x] In `AOnsetPlayerController.h`: `UPROPERTY()` `UTargetingComponent* TargetingComponent`
- [x] In constructor: `TargetingComponent = CreateDefaultSubobject<UTargetingComponent>(TEXT("TargetingComponent"))`

## 3. IA_Primary Context Resolution
- [x] `OnPrimaryInteraction()` branches on `"Enemy"` tag → `SetTarget`, ground → `MoveToLocation` + `ClearTarget`

## 4. Implement IsActorValidTarget()
- [ ] Add method to `UTargetingComponent`: `bool IsActorValidTarget(AActor* Target)` 
- [ ] Default implementation: return `Target != nullptr && Target != GetOwner()` (PvP filtering added later)
- [ ] Call inside `SetTarget()` before assigning

## 5. Stub an Enemy for Testing
- [ ] Create a temporary Blueprint or C++ actor with the `"Enemy"` tag, place in level

## 6. Test Basic Attack (stub)
- [ ] Bind `IA_Ability1` in `SetupInputComponent()` → `OnBasicAttack()` handler
- [ ] Handler logs target name if `CurrentTarget` exists, else logs "No target"

## 7. Test
- [ ] Compile → Play
- [ ] Click/tap enemy → target is set
- [ ] Click/tap ground → character moves, target clears
- [ ] Press ability key → target name appears in log if target set
- [ ] Gamepad R-Stick cursor over enemy → A button to target → ability button to attack

## ✅ Done
- [ ] Check off **A1.4** in `TODO/Private_Demo_Checklist.md`
