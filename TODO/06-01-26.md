# 🗓 MON 1 JUN — A3.1 StateTree Schema

## 1. Create UNPCStateTreeSchema
- [ ] Create `UNPCStateTreeSchema` — `UCLASS()`, inherits `UStateTreeSchema` with context data
- [ ] Define context struct: self (FInstancedStruct), target (AActor*), group data (FGroupData), health (float)
- [ ] Expose context as evaluators for StateTree conditions

## 2. Bind NPC Context
- [ ] In `AOnsetAIController` (or a new context provider class), populate context data on tick/event:
  - [ ] Self → possessed pawn
  - [ ] Target → from TargetingComponent
  - [ ] Group data → from UGroupComponent → GroupManager → GetGroupData()
  - [ ] Health → from GAS AttributeSet (stub for now, A4)
- [ ] Feed context into StateTree before each tick

## 3. Verify
- [ ] Compile → Play
- [ ] StateTree starts on NPC spawn
- [ ] Debug display shows current AI state
- [ ] Add on-screen debug widget (or log) for state transitions

## ✅ Done
- [ ] Check off **A3.1** in `TODO/Private_Demo_Checklist.md`
