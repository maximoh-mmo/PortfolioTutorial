# 🗓 SUN 07 JUN — A3.3 Flee (Polish) + A4.5b Corpse System + PoolManager → Subsystem Migration

## ✅ Done

### A3.3 Flee State (Polish)
- [x] `FOnsetStateTreeFleeCondition` — multi-factor flee decision:
  - Reads FleeThreshold from AIProfile automatically
  - Group courage bonus (AliveCount within support radius raises threshold)
  - FleeProbability gate (only X% of eligible enemies actually flee)
- [x] `FOnsetStateTreeFleeTask` — flee movement with dynamic speed:
  - Flee direction away from target + random angle variance (±60°)
  - Navmesh projection near HomeTransform
  - Health-proportional speed (MinSpeedMultiplier → 1.0f by health ratio)
  - Speed restored on ExitState
- [x] `GetSelfEnemyCharacter()` helper added to `FOnsetStateTreeTaskBase`
- [x] GE_Stagger BP created, stagger applied in GA_HitReaction
- [x] StateTree asset wired: Flee state + FleeCondition
- [x] AIProfile FleeThreshold configured

### A4.5b Corpse System
- [x] `AOnsetCorpse` — minimal replicated actor, cube static mesh, QueryOnly collision on ECC_Visibility, SetLifeSpan despawn
- [x] `UOnsetCorpseSubsystem` — UWorldSubsystem with configurable MaxActiveCorpses (20) + CorpseLifespan (15s)
- [x] Cap enforcement: lazy sweep of dead weak ptrs, oldest eviction when at cap
- [x] `AOnsetEnemy::OnDeath()` spawns corpse via subsystem before deferred pool cleanup

### PoolManager → PoolSubsystem Migration
- [x] `AOnsetPoolManager` replaced by `UOnsetPoolSubsystem` (UWorldSubsystem)
- [x] `PoolSize` configurable via `EditAnywhere` on subsystem CDO
- [x] `AOnsetSpawner`: removed `PoolManager` member, uses `GetWorld()->GetSubsystem<>()` in all functions
- [x] Old `OnsetPoolManager.h/.cpp` deleted
- [x] Removed dead `Spawning/OnsetPoolManager.h` include from `OnsetEnemy.cpp`

### Documentation
- [x] Episode_List.md: Inserted Episode 21 — Architecture Cleanup (Interaction Component + Profile Split)
- [x] Episodes 22-38 renumbered to 23-39
- [x] Production_Timeline.md: Added Phase 3.5, updated episode ranges, added milestone
- [x] Scope_Overview.md: 38→39 episodes, added Phase 3.5/3.6
- [x] Series_Overview.md: Added Architecture Cleanup phase
- [x] Private_Demo_Checklist.md: Marked Flee/Stagger/Corpse items done, updated progress stats
- [x] Corpse_System.md: Archived as implemented (design doc reflects shipped state)

## Verified
- [ ] Flee state: NPC flees when health below threshold
- [ ] Flee variance: ±60° direction scatter visible
- [ ] Flee speed: slowing as health decreases
- [ ] Corpse spawns on NPC death, despawns after lifespan
- [ ] Corpse cap enforcement works (evicts oldest)
- [ ] PoolSubsystem migration: spawners still function, NPCs return to pool
- [ ] Compilation: clean build after all changes

## What's Next
- A3.4 — Group Assist (FAINoiseEvent on damage → hearing → StateTree)
- A4.2 — Animation montage (simple melee swing)
- A4.6 — Multiple abilities (dash, AoE, projectile)
- Episode 21 refactor (InteractionComponent + Profile Split)
