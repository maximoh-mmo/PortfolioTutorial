# 🗓 SUN 07 JUN — A3.4 Group Assist (Noise Events + Investigation)

## ✅ Done (this session)

### A3.4 Group Assist — Noise Event Emission
- [x] `UAISense_Hearing::ReportNoiseEvent` in `PostGameplayEffectExecute` on damage taken
- [x] `OnsetAttributeSet.cpp`: `#include "Perception/AISense_Hearing.h"`, `Loudness = damage magnitude`, `Instigator = damaged actor`, `MaxRange = 0`

### A3.4 — Controller Noise Storage + Perception Split
- [x] `AOnsetAIController.h`: added `HeardNoiseLocation`, `HeardNoiseInstigator`, `bHasPendingNoise`, `LastNoiseHeardTime`
- [x] `AOnsetAIController::OnPerceptionUpdated`: split sight vs hearing — sight sets `TargetingComponent`, hearing stores closest noise on controller fields
- [x] `OnUnPossess`: clears all noise fields
- [x] `else { bHasPendingNoise = false; }` when no actors currently heard

### A3.4 — HearingCondition (gates Idle → Investigate)
- [x] `FOnsetStateTreeHearingConditionInstanceData` with `MaxTimeSinceLastNoise` (designer-tunable)
- [x] `TestCondition`: checks `bHasPendingNoise` + noise expiry time

### A3.4 — InvestigateTask (move to noise location)
- [x] `FOnsetStateTreeInvestigateTaskInstanceData`: `GroupMemberSpeedMultiplier` (×1.0), `NonGroupSpeedMultiplier` (×0.5), `AcceptanceRadius`
- [x] `EnterState`: caches `MaxWalkSpeed`, checks group membership via `GroupComponent`, applies speed, moves to `HeardNoiseLocation`
- [x] `Tick`: exits on enemy sighted/noise expired, repaths on destination change, checks move completion
- [x] `ExitState`: restores cached speed, stops movement

### Documentation
- [x] Created `Docs/GAS/Episode22_MovementSpeedAttribute.md` — full episode outline for refactoring direct speed → GAS attribute
- [x] `Episode_List.md`: Inserted Episode 22 (GAS Movement Speed Attribute), Episodes 23-39 renumbered to 24-40
- [x] `Series_Overview.md`: 39→40 episodes, updated Phase 3.5 description
- [x] `Production_Timeline.md`: Updated all episode ranges, cadence, milestones, totals for 40 episodes
- [x] `Private_Demo_Checklist.md`: Marked 6 A3.4 items done

## In Progress
- None — awaiting next task

## Remaining (A3.4)
- [ ] SearchTask (Alerted state — yaw sweep scan at noise origin)
- [ ] StateTree BP wiring: Idle → HearingCondition → Investigate → Search → Idle

## What's Next (from previous)
- A3.4 — Group Assist (SearchTask + StateTree wiring)
- A4.2 — Animation montage (simple melee swing)
- A4.6 — Multiple abilities (dash, AoE, projectile)
- Episode 21 refactor (InteractionComponent + Profile Split)
- Episode 22 — GAS Movement Speed Attribute (episode doc written, implementation deferred)
