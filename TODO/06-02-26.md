# 🗓 TUE 2 JUN — A3.2 AI Perception

## 1. Implement OnPerceptionUpdated()
- [ ] In `AOnsetAIController.cpp` — fill the empty `OnPerceptionUpdated()` body (line 77)
- [ ] On sight: if perceived `AActor*` is a valid player → set StateTree target + trigger Agro transition
- [ ] On hearing: if noise source is an ally (check group membership via `UGroupComponent`) → set StateTree assist flag
- [ ] Handle stimulus expiry (lost sight → clear target → trigger Lost transition)

## 2. Feed Perception into StateTree Context
- [ ] Wire `OnPerceptionUpdated` results into the StateTree context (target actor, assist flag)
- [ ] Ensure perception data is available to StateTree evaluators/conditions

## 3. Verify
- [ ] Compile → Play
- [ ] Player walks near NPC → perception triggers → Agro fires
- [ ] Player leaves sight → Lost fires
- [ ] (Noise-based assist tested in A3.4)

## ✅ Done
- [ ] Check off **A3.2** in `TODO/Private_Demo_Checklist.md`
