# 🗓 TUE 2 JUN — A1.5 Ability Targeting System

## 1. Create AbilityTargetData Struct (BlueprintType)
- [ ] `Source/Onset/Public/Combat/AbilityTargetingTypes.h`
- [ ] `USTRUCT(BlueprintType)` `FAbilityTargetData` with: `AActor* TargetActor`, `FVector TargetLocation`, `FVector TargetDirection`, `float Radius`

## 2. Create UAbilityTargetingComponent (Blueprintable + BlueprintSpawnableComponent)
- [ ] `Source/Onset/Public/Combat/UAbilityTargetingComponent.h` — `UCLASS(Blueprintable, BlueprintType, meta=(BlueprintSpawnableComponent))`, inherit `UActorComponent`
- [ ] Methods:
  - `FAbilityTargetData GetTargetUnderCursor()` — raycast for actors (reads from UCursorManager)
  - `FAbilityTargetData GetGroundLocationUnderCursor()` — raycast for ground (reads from UCursorManager)
  - `FAbilityTargetData GetDirectionFromPlayerToCursor()` — direction vector from player to cursor
  - `void ShowTargetIndicator(const FAbilityTargetData&)` — spawn decal/sphere
  - `void HideTargetIndicator()`
- [ ] `Source/Onset/Private/Combat/UAbilityTargetingComponent.cpp` — implement all methods

## 3. Add to PlayerController
- [ ] In `AOnsetPlayerController.h`: `UPROPERTY()` `UAbilityTargetingComponent* AbilityTargeting`
- [ ] In constructor: `AbilityTargeting = CreateDefaultSubobject<UAbilityTargetingComponent>(TEXT("AbilityTargeting"))`

## 4. Test (all cursor sources)
- [ ] Compile → Play
- [ ] Point cursor at enemy → verify single-target data (mouse, touch tap, gamepad R-Stick)
- [ ] Point at ground → verify AoE location data
- [ ] Move cursor away from player → verify direction vector
- [ ] Indicator appears/disappears

## ✅ Done
- [ ] Check off **A1.5** in `TODO/Private_Demo_Checklist.md`
