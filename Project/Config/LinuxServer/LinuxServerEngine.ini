; Linux dedicated-server-only configuration.
; These sections are ONLY packaged into LinuxServer builds — never into clients.
; Prod secrets must be injected at deploy time (env / CI); values below are dev placeholders.

[Onset.DataStore]
Type=HttpApi
ConnectionString=qnghmsigrompw56v5wrlojl7z40dwtqm.lambda-url.us-east-1.on.aws/
APIKey=dev-api-key-change-me-in-production

[Onset.Auth]
AuthTokenSecret=change-me-in-production
AuthTokenLifetimeSeconds=300
